{{! template-lint-disable no-action }}
<@body.row class='person' as |row|>
  <row.cell>
    <PaperSwitch
      @value={{this.person.active}}
      @onChange={{action 'toggleActiveness'}}
    />
  </row.cell>
  <row.cell class='name'>
    {{this.person.name}}
  </row.cell>
  <row.cell class={{this.emailClass}}>
    <a href='mailto:{{this.person.email}}'>
      {{this.person.email}}
    </a>
  </row.cell>
  <row.cell class={{this.mobileClass}}>
    {{#if this.person.mobile}}
      <a href='tel:{{this.person.mobile}}'>
        {{this.person.mobile}}
      </a>
    {{/if}}
  </row.cell>
  <row.cell class={{this.landlineClass}}>
    {{#if this.person.landline}}
      <a href='tel:{{this.person.landline}}'>
        {{this.person.landline}}
      </a>
    {{/if}}
  </row.cell>
  <row.cell>
    {{#if this.person.address}}
      <CopyButton @text={{this.person.address}} @onSuccess={{action 'copied'}}>
        {{paper-icon 'content copy'}}
      </CopyButton>
    {{/if}}
  </row.cell>
  <row.cell class='last-ride'>
    {{#if this.person.lastRide}}
      {{moment-format this.person.lastRide.start 'MMMM D, YYYY'}}
    {{/if}}
  </row.cell>
  <row.cell class='notes'>
    {{this.person.notes}}
  </row.cell>
  <row.cell>
    <PaperButton
      @iconButton={{true}}
      @aria-label='Edit person'
      @title='Edit person'
      class='edit'
      @onClick={{action this.editPerson this.person}}
    >
      {{paper-icon 'mode edit'}}
    </PaperButton>
  </row.cell>
</@body.row>