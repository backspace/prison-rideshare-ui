{{#paper-toolbar as |toolbar|}}
  {{#toolbar.tools}}
    {{#paper-button iconButton=true onClick=(action 'toggleSidebar') class='hide-gt-sm'}}
      {{paper-icon "menu"}}
    {{/paper-button}}
    <h2>{{title}}</h2>
    {{! FIXME restore sandbox chip }}
    <span class='flex'></span>
    {{yield}}
  {{/toolbar.tools}}
{{/paper-toolbar}}
